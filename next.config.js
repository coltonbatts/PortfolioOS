/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Markdown files are loaded server-side, no special webpack config needed
  // Future: Add image optimization domains, API routes for AI integration
}

module.exports = nextConfig
